#V2
USE NS Dispatcher;
USE DB DispatcherDB;
INFO FOR DB;
SELECT * FROM role;
USE NS Dispatcher;
USE DB DispatcherDB;
INFO FOR DB;
SELECT * FROM role;
USE NS Dispatcher;
USE DB DispatcherDB;
INFO FOR DB;
SELECT * FROM role;
SELECT * FROM ->session;
USE NS Dispatcher;
USE DB DispatcherDB;
INFO FOR DB;
SELECT * FROM role;
USE NS Dispatcher;
USE DB DispatcherDB;
INFO FOR DB;
SELECT * FROM role;
USE NS Dispatcher;
USE DB DispatcherDB;
INFO FOR DB;
SELECT * FROM role;
USE NS Dispatcher;
USE DB DispatcherDB;
INFO FOR DB;
SELECT * FROM role;
SELECT id, <-participateIn<-human.name, <-participateIn<-interroagtor.name, <-participateIn<-computer.model, ->includes->answer.text, ->includes->question.text, ->includes->verdict.correct FROM session;
USE NS Dispatcher;
USE DB DispatcherDB;
INFO FOR DB;
SELECT * FROM role;
CREATE session:1;
CREATE human:1 SET name = "John";
RELATE human:1->participateIn->session:1;
SELECT id, <-participateIn<-human.name, <-participateIn<-interroagtor.name, <-participateIn<-computer.model, ->includes->answer.text, ->includes->question.text, ->includes->verdict.correct FROM session;
USE NS Dispatcher;
USE DB DispatcherDB;
INFO FOR DB;
SELECT * FROM role;
USE NS Dispatcher;
USE DB DispatcherDB;
INFO FOR DB;
SELECT * FROM role;
USE NS Dispatcher;
USE DB DispatcherDB;
INFO FOR DB;
SELECT * FROM role;
USE NS Dispatcher;
USE DB DispatcherDB;
INFO FOR DB;
SELECT * FROM role;
USE NS Dispatcher;
USE DB DispatcherDB;
INFO FOR DB;
SELECT * FROM role;
RELATE human:2->participateIn->session:1;
SELECT id, <-participateIn<-human.name, <-participateIn<-interroagtor.name, <-participateIn<-computer.model, ->includes->answer.text, ->includes->question.text, ->includes->verdict.correct FROM session;
USE NS Dispatcher;
USE DB DispatcherDB;
INFO FOR DB;
SELECT * FROM role;
CREATE human:2 SET name = "BOB";
RELATE human:2->participateIn->session:1;
SELECT id, <-participateIn<-human.name, <-participateIn<-interroagtor.name, <-participateIn<-computer.model, ->includes->answer.text, ->includes->question.text, ->includes->verdict.correct FROM session;
USE NS Dispatcher;
USE DB DispatcherDB;
INFO FOR DB;
SELECT * FROM role;
CREATE session:2;
RELATE human:2->participateIn->session:2;
SELECT id, <-participateIn<-human.name, <-participateIn<-interroagtor.name, <-participateIn<-computer.model, ->includes->answer.text, ->includes->question.text, ->includes->verdict.correct FROM session;
